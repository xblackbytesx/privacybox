version: '3'

networks:
  proxy:
    name: proxy
    external: true

volumes:
  bitwarden-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${DOCKER_ROOT}/bitwarden/data

services:
  bitwarden:
    container_name: bitwarden
    image: vaultwarden/server:alpine
    environment:
      - WEBSOCKET_ENABLED=true
      - SIGNUPS_ALLOWED=true
      - DOMAIN=https://${SUBDOMAIN}.${DOMAIN}
    volumes:
      - bitwarden-data:/data
    labels:
      - traefik.enable=true
      - traefik.http.routers.bitwarden.entrypoints=web
      - traefik.http.routers.bitwarden.rule=Host(`${SUBDOMAIN}.${DOMAIN}`)
      - traefik.http.routers.bitwarden-secure.entrypoints=websecure
      - traefik.http.routers.bitwarden-secure.rule=Host(`${SUBDOMAIN}.${DOMAIN}`)
      - traefik.http.routers.bitwarden-secure.tls=true
      - traefik.http.routers.bitwarden-secure.tls.certresolver=le-dns
      - traefik.http.routers.bitwarden-secure.service=bitwarden
      - traefik.http.routers.bitwarden-secure.middlewares=https-redirect@file,non-www@file,secure-headers@file,gzip-compress@file,basic-auth@file
      - traefik.http.services.bitwarden.loadbalancer.server.port=80
      - traefik.http.routers.http-catchall.rule=HostRegexp(`{any:.+}`)
      - traefik.http.routers.http-catchall.entrypoints=web
      - traefik.http.routers.http-catchall.middlewares=https-redirect@file
    networks:
      - proxy
    restart: unless-stopped
